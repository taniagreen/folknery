import React from 'react';
// import GoogleMap from 'google-map-react';

class Map extends React.Component {
 /* componentDidMount() {
    const myCenter = new GoogleMap.LatLng(-33.874066, 151.207517);
    function initialize() {
      const mapProp = {
        center: myCenter,
        zoom: 12,
        scrollwheel: false,
        draggable: false,
        mapTypeId: GoogleMap.MapTypeId.ROADMAP,
      };

      const map = new GoogleMap.Map(document.getElementById('googleMap'), mapProp);

      const marker = new GoogleMap.Marker({
        position: myCenter,
      });

      marker.setMap(map);
    }

    GoogleMap.event.addDomListener(window, 'load', initialize);
  }*/

  render() {
    return (
      <div />
    );
  }
}

export default Map;

